{
  "name": "vite-plugin-fastly-server-only-example",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite dev",
    "build": "vite build",
    "preview": "fastly compute serve --file ./dist/app.wasm",
    "deploy": "fastly compute pack -w ./dist/app.wasm && fastly compute deploy"
  },
  "dependencies": {
    "@fastly/cli": "^14.2.0"
  },
  "devDependencies": {
    "@fastly/js-compute": "^3.41.1",
    "vite": "^8.0.8",
    "vite-plugin-fastly": "0.1.0"
  }
}
